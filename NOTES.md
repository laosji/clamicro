# 维护笔记

给未来的自己。记的是**踩过的坑和当时的判断依据**，不是使用说明——那部分在 [README](./README.md)。

---

## 发布

### npm 11 的浏览器登录令牌没有发布权限

`npm login` 走浏览器 OAuth，终端确实会打印 `Logged in on https://registry.npmjs.org/`，`npm whoami` 也能返回用户名。但一发布就是：

```
403 Forbidden - Two-factor authentication or granular access token
with bypass 2fa enabled is required to publish packages.
```

**`npm publish --otp=xxxxxx` 也救不了**，试过。而且 npm 11 在这种令牌下根本不会弹出 OTP 交互提示，直接失败。

出路只有一条：去 https://www.npmjs.com/settings/<user>/tokens/new 建 **Granular Access Token**，勾上 **Bypass 2FA**，权限选 Read and write，然后

```bash
npm config set //registry.npmjs.org/:_authToken=<令牌>
```

包发布之后记得回去把令牌的作用域从 All packages 收窄到这一个包——泄露时影响面差别很大。

另一条路是把账号 2FA 从 *Authorization and Publishing* 降到 *Authorization only*，但那是把整个账号的发布保护关掉，为一次发布不值得。

### 发布前必须解包实物 tarball 搜密钥

`npm pack --dry-run` 只列文件名，看不到内容。真正的检查是解开打好的包逐字搜：

```bash
npm pack --pack-destination /tmp/v && tar xzf /tmp/v/*.tgz -C /tmp/v
grep -rl "<你的真实token>" /tmp/v/package
```

**这条救过一次**：`ui/settings.html` 里输入框的 placeholder 当初直接写了真实的 Bark key（`placeholder="例如 rNJf…"`）。发出去等于把推送通道公开，任何人都能往那台手机推东西。同类问题还有注释里的真实内网 IP 和主机名。

### `npm publish` 报 403 不一定是没发出去

实测到的：`npm publish` 上传很慢（网络抖），客户端超时后重试，而**第一次
其实已经成功了**，于是第二次撞上自己：

```
npm error 403 You cannot publish over the previously published versions: 2.17.2
```

看着像失败，其实是成功。**别信退出码，去 registry 上验实物**：

```bash
npm view clamicro version
npm pack clamicro@<版本> && tar xzf clamicro-<版本>.tgz
```

反过来也要防：真失败时不要因为「反正报的是 403」就当成发过了。判据永远是
registry 上那份包里有没有你这次的改动——所以解包之后除了搜密钥，还要
**逐条确认这一版的改动在、前几版的修复没退版**。连发几个版本时后者尤其
容易出事。

### 版本号一旦用掉就永久占用

即使 unpublish 也不能再发同号。24 小时后连 unpublish 都不行，只能 deprecate。

---

## 架构上不能忘的几件事

### 目录约定

```
server.mjs        入口。**必须留在根目录**——已安装的 hooks 里写的是
                  ~/.claude/clamicro/app/server.mjs 这个绝对路径，挪了就断
cli.mjs           CLI 入口（package.json 的 bin）
install.mjs       安装器
bin/*.sh          hook 脚本。同样被绝对路径引用，不能挪
src/
  http/           收发与传输层安全（respond / security）
  auth/           口令校验
  risk/           风险判定：危不危险。规则表与逻辑分开
  view/           展示：怎么说清楚。(toolName, toolInput) → 展示结构的纯函数
  routes/         按职责分的路由（hooks / pages / api）
  *.mjs           领域与基础设施（approvals / state / config / …）
ui/               页面
test/             不进 npm 包
```

`risk/` 和 `view/` 是一对：前者判「危不危险」，后者管「怎么说清楚」。
分开的好处在 `view/describe.mjs` 上很明显——它原本埋在 ApprovalStore 里，
只能通过 `create()` 间接测；拆出来之后边界情况（空输入、坏 URL、
超长描述、截断上限）才有人管。

**`state.mjs` 没有拆，是有意的。** 它 337 行主要是 `applyHook` 那个大 switch，
而 switch 全程在动 `#sessions` / `#log` / `#touch` 这些私有字段——抽出去就得
把内部状态暴露成公开 API，那是拿封装换行数，更差。行数大不等于该拆，
要看有没有真的接缝。

路由模块统一是「工厂 + 返回 handler」：`routes(ctx)` 拿到依赖，返回
`(req, res, url, path) => boolean`，**返回 true 表示已处理**。加新端点时
放进对应的 routes 文件，别往 server.mjs 里塞——它现在只负责装配和启动。

`src/risk/` 单独拎出来是因为它是安全核心：判定错了，上层再好的 UI、
再稳的传输都没有意义。独立之后测试可以直接打在它身上，不必起服务。

一个坑：`server.mjs` 装配路由时不能直接传 `net` / `trusted`，它们在
下面的「网络信任闸门」一节才求值，直接传会撞 TDZ。传闭包（`network: () => …`），
闭包体到请求进来时才执行。

### npm 包只是安装器

运行时必须复制到 `~/.claude/clamicro/app/`，hooks 指向那里。

hooks 里存的是**绝对路径**，而 `npx` 跑在会变的缓存目录、全局安装路径随 node 版本 / nvm / homebrew 变化。指向 npm 包意味着某天路径失效，**所有 hook 静默失败**——不报错，只是再也收不到通知。

验证方式：装完删掉 `node_modules`，服务和 hook 应该照常工作。

### hooks 热加载，statusLine 不

改 hooks 当前会话立刻生效；statusLine 是**会话启动时读一次**。

所以改名或换安装路径之后，**已经开着的会话 statusLine 会静默失效**——表现是那个会话再也不上报额度，而且没有任何报错。只能新开会话。

### 服务的生命周期跟着 Claude Code 走

监听套接字绑在**进程启动那一刻**的局域网 IP 上。DHCP 续租、换 Wi-Fi 之后那个
地址不再属于本机，于是：进程还活着、`/healthz` 照常回 ok、终端里一片安静，
但手机连过来是超时。

曾经想加个 20 秒的后台巡检来动态重绑，砍掉了——服务本来就是被 SessionStart
hook 拉起来的，生命周期已经跟着 Claude Code 了，再加一条独立的心跳是第二套
状态机。现在 `/healthz` 多返回一个 `stale`（现场重新探测 IP 和启动时比对），
`bin/session-start.sh` 看到 `stale:true` 就杀掉重启。顺带让网络信任闸门也
重新判一遍当前网络。

**已知残留**：如果换到一个不同的网络但**恰好拿到相同的 IP**，且中途不新开
Claude Code 会话，信任闸门不会重新跑，局域网监听会一直开着。窄，但不是零。
真要堵死得靠巡检或者 macOS 的网络变化通知。

### PermissionRequest 的 payload 里没有 tool_use_id

实测两个 hook 收到的字段（Claude Code 2.1.x）：

```
PermissionRequest  session_id transcript_path cwd prompt_id permission_mode
                   effort hook_event_name tool_name tool_input
                   permission_suggestions
PreToolUse         …同上（无 permission_suggestions）+ tool_use_id
PostToolUse        …PreToolUse 的全部 + tool_response duration_ms
```

**权限阶段还没分配 `tool_use_id`**，要到 PreToolUse 才有。所以「把挂起的审批和已经开跑的工具调用对上号」不能用它——第一版就是这么写的，结果 `supersede()` 是段永远匹配不上的死代码，幽灵条目照旧。

能用的只有两边都在、且逐字相同的三元组：`session_id` + `tool_name` + `tool_input`。哈希成 `match_key` 存在审批记录上，PreToolUse/PostToolUse 拿同样的三元组算一次去销账。

两个坑：

- `tool_input` 要**递归排序键后再序列化**。同源对象的键顺序本该一致，但那是「碰巧成立」，不值得赌。
- 同一会话里可能连着跑两条一模一样的命令，键会撞。一次只销**最早**的那条，剩下的留给下一次对账——否则两条挂起会被一次全销掉。

### 只能在服务端验证的东西，不代表客户端也成立

曾经把 `.local` 主机名设成默认地址，理由是「换 IP 不用重扫码」，并且在 Mac 上 `curl http://<host>.local:8765/healthz` 返回了 200，以为验证通过了。

**那个测试毫无意义**——Mac 解析自己的 `.local` 走的是回环，永远成功。手机能不能通过 mDNS 解析是另一回事，受管网络常屏蔽组播。结果就是二维码扫了打不开。

现在默认是 IP，`.local` 由**手机自己**发一个真实请求去探测，通了才建议切换。

同类判断：拿硬故障换小麻烦是亏的。打不开 = 完全不能用，IP 变了 = 重扫一次。

### 配对页里做不了「打开摄像头扫码」

提过一次，值得记下来免得再提：**两层硬阻断，不是没做。**

1. `getUserMedia` 要求 **secure context**。服务跑在 `http://<局域网IP>:8765`，
   既不是 HTTPS 也不是 localhost，iOS Safari 里 `navigator.mediaDevices`
   直接是 `undefined`。和「拿不到 Service Worker / Web Push」是同一个根因。
2. 就算有 HTTPS（`tunnel on` 那条路），**Safari 没有 `BarcodeDetector`**，
   解码得自带一个 QR 库，和运行时零依赖的约定冲突。

唯一能绕过第 1 层的是 `<input type="file" accept="image/*" capture="environment">`
——它是文件选择器不是 getUserMedia，不受 secure context 限制。但第 2 层还在，
而且交互退化成「拍一张再解码」，比原生相机的实时取景差一档。

更根本的是**方向不对**：配对页在手机上、二维码在 Mac 上，而 iOS 相机 App
扫完会直接开一个 Safari 标签页跳过去——那本来就是完整且更好的路径。
用户缺的不是「扫码按钮」，是「不知道该拿什么去扫」。所以答案是一行小字，
不是一个摄像头。

---

## 开发时会自己卡自己

`config.ignoreCwds` 里的工作目录跳过阻塞审批。开发本项目时把项目目录放进去，否则每跑一条命令都在等自己审批——**高危命令（`rm -rf` 之类）会挂满 570 秒**，而那时候没人在看手机。

注意匹配的是**会话的 cwd**，不是命令里 `cd` 到哪。踩过两次：填了 `.../clamicro` 而会话 cwd 是它的父目录 `~/Downloads`，等于没填。正确的写法是会话 cwd 本身。

`clamicro status` 现在会把免审批目录列出来并标黄——它是**完全旁路**，那些目录里 `rm -rf` 直接执行，这种事不该看不见。

发布前记得清空。

---

## 那些"看起来能跑"但其实错的

按踩到的顺序：

| 症状 | 真因 |
|---|---|
| 页面渲染出自己的源码 | `String.replace` 的替换串里 `$'` 表示"匹配之后的全部内容"，而审批内容全是 shell 命令、`$` 遍地都是。必须用函数式替换 |
| 一次 20px 的拖动就批准了操作 | `window.innerWidth` 在页面后台渲染时可能返回 **0**，阈值算出 0，任何位移都过线。阈值要有下限，另加绝对位移底线 |
| 从推送点进来每次都要重新扫码 | cookie 用了 `SameSite=Strict`。从外部 App 跳转属于跨站导航，Strict 的 cookie 不会被带上。必须 `Lax` |
| 取消之后下一个工具调用也被取消 | `cancel()` 唤醒挂起的 waiter 后没复位状态，`CANCELLED` 留着被下一次消费 |
| 额度反复变空 | 额度是纯内存的，没进 `history.json`，每次重启就清零 |
| 卸载后残留一个 hook | 卸载用路径片段 `clamicro/bin/` 匹配，而目录结构改过（`app/bin/`）。改成按脚本文件名认 |
| 界面显示「待审批」，可操作其实早执行完了 | 授权在别处放行后（终端弹框、权限规则、`acceptEdits` 模式），Claude Code **不会取消**已发出的 PermissionRequest 请求，连接还开着、close 事件也不触发，记录挂到超时。用 PreToolUse 对账销掉并返回 allow——但对账键**不能用 `tool_use_id`**，见下 |
| 首页 tab 高亮，内容却是另一个 tab 的 | 渲染函数抛异常时 `innerHTML` 保留上一个 tab 的内容，界面看起来正常只是对不上。起因是加变量时替换没匹配上、声明漏了，赋值给未声明变量抛 ReferenceError。已加渲染兜底：出错就显式报错，不要静默留着别人的内容 |
| `npm i` 后 CLI 报 import 失败 | `package.json` 的 `files` 白名单漏了 `install.mjs`。这种错只有真的打包安装才会暴露 |
| 二维码和推送深链全指向一个打不开的域名 | `publicBaseUrl` **落盘持久化**，而 quick tunnel 的地址是**临时**的。隧道进程一没（重启、被杀、Cloudflare 断开），配置里那个地址就作废了，但 `baseUrl` 仍无条件用它。现在以**进程是否存在**为准（`tunnelAlive()` 读 pid 文件 + `kill(pid, 0)`），死了就回落局域网并打一行日志 |
| `networks` 显示「网关 10.x.x.x  网关 10.x.x.x」 | 拿不到 SSID 时 `fp.label` 本身就是 `网关 ${gateway}`，输出又拼了一次网关 |
| 管道输入时安装程序挂死 | `printf 'y\ny\n' \| node install.mjs` 会一次性 EOF，readline 把所有行同时发完，第二行在第二次提问注册前就丢了。要自己缓冲行队列 |
| 删了函数却没删调用点，非法 Host 抛异常走 400 而不是 403 | `node --check` 只查语法，查不出未定义引用。功能上仍然拒绝，但那是碰巧——异常路径不该是安全边界的实现方式。这类只有跑起来才暴露，所以 HTTP 层要有集成测试 |
| 用 fetch 测 DNS rebinding，测了个寂寞 | `Host` 是 forbidden header name，规范要求实现忽略调用方设置的值。头被静默丢弃、请求带着真实 Host 发出去，于是「伪造 Host 被拒」这条断言实际测的是「合法 Host 被放行」。要用 `node:http` 原生请求 |
| 改代码时把页面源码注入进了源文件 | 用 `String.replace` 做代码替换，而替换串里写了 `` ——就是那段注释本身在警告的事。**任何 replace 都用函数式**，替换串里的 `$` 一律不解释 |
| 换网络后手机再也连不上，`status` 显示一切正常 | `bind` 里的 `null` 是「启动时探测局域网 IP」的占位符，而 `saveConfig` 把探测**结果**写回了盘。之后那个地址不属于本机，`listen` 报 `EADDRNOTAVAIL`，服务只剩回环。触发点是 `clamicro trust`——那条**专门用来开放局域网访问**的命令。查了两天 |
| 手机页面停在旧状态，看着像服务卡死 | Mac 换 IP 后，页面还连着旧地址，SSE 断了。原来只把一个小圆点的 `live` class 去掉，手机上根本看不见 |
| 点按钮报 `notify is not a function` | 装配处漏传依赖，解构出 `undefined`。JS 不在那一刻报错，要等真的调用才炸。已加 `requireDeps`，改成启动时就失败 |
| 刘海胶囊代码全对，屏幕上什么都没有 | 见下面「JXA 画窗口」一节，一共四个独立的坑，每一个都单独足以让它彻底不显示，而且**四个都不报错** |
| 手机上问「修改 auth.ts，批准吗」，而你看不到改了什么 | Edit / Write 那一支的 `detail` 只有文件路径。Bash 那一支把命令原文当主视觉的理由（模型写的描述不可信）对写文件完全成立，却漏做了一半 |
| 批准一条待审批，会话就显示「运行中」——其实还卡着 | Claude Code 会并行发起工具调用，一个会话同时挂两条 PermissionRequest 是常态。`clearWaitingApproval` 只认 session_id、不认是哪一条结束了。**成对的开始/结束事件遇上并发就得计数，不能用布尔** |
| 手机上写着「已拒绝」，而拒绝从没到达任何地方 | `responded` 标志置位的时机是「拿到决定」，不是「答复写出去了」。连接在那之前断掉的话，`json()` 写进死 socket，而记录写着 decided_by: phone |
| 终端里跑一次 `trust`，手机上刚改的设置悄悄回滚 | 短命 CLI 是「启动读整份配置 → 干完整份写回」，服务那边也整份写回。后写者赢。更狠的是 `pruneDefaults` 只写非默认值，CLI 快照里那个键还是默认值，于是被**剪掉**——连痕迹都不剩 |
| VPN 一开，网络指纹只剩网段，而「辨识度低」的告警不响 | 全隧道 VPN 接管默认路由 → `route` 给出点对点 utun、没有网关行 → 网关/MAC/SSID/DHCP 六项一起消失。而 `weak` 的判据写的是「网关 MAC 是 VRRP 虚拟地址」，MAC 是 null 时它判不成立——**恰恰在指纹最弱的时候说它不弱** |
| 一个被 `kill -9` 的会话永远停在「运行中」 | 会话只在 session-end 时才从表里消失，而硬杀不发那个事件；5 分钟巡检扫审批、history、hooks、日志，唯独不扫会话 |

### JXA 画窗口：四个各自足以让它彻底不显示的坑

做刘海 HUD 时连着踩了四个。共同点是 `osascript` 退出码 0、日志正常、`isVisible` 返回 true，**只有肉眼看屏幕才知道没画出来**。

1. **必须跑 `NSApp.run`，不能手动步进 runloop。**
   原来用 `NSRunLoop.currentRunLoop.runUntilDate()` 一帧帧推。窗口建得出来，WindowServer 也分配了 windowNumber，`isVisible=true`——但 `occlusionState` 始终是 8192，不含 visible 位（2）。换成 `NSApp.run` 之后立刻变 8194，画面就出来了。**AppKit 窗口要参与合成，得有 AppKit 自己的事件循环在跑。**
   配套：结束时不能用 `app.stop()`，它要等下一个事件才跳出循环，而这个进程没有任何输入事件 → 实测挂 60 秒不退。一次性进程直接 `app.terminate(null)`。
   还有：`app.run` 不能是 `run()` 的尾表达式，JXA 会把它当返回值求值，实测那样根本不进事件循环。后面随便跟一条语句就行。

2. **`spawn` 不能加 `detached: true`。**
   它会 `setsid()` 把子进程放进新会话，那个会话拿不到 WindowServer。终端里直接跑能弹，从 node 用 detached 起就不弹。

3. **不能用 `$.NSColor.blackColor.CGColor`。**
   返回的是 autorelease 的 CGColor，JXA 不替你持有，等图层真正绘制时已经悬空 → 进程被 SIGKILL，130ms 就没，**stderr 一个字都没有**。用 `$.CGColorCreateGenericRGB(0,0,0,1)`（+1 持有）。

4. **父进程别在 HUD 播完前退出。**
   `showHud` 之前调了 `unref()`，而 `--test-push` 走完 notify 就 `process.exit(0)`。子进程跟着被带走，于是这个**自检命令**打印「提醒通道是通的」，屏幕上什么都没出现过。去掉 unref，并让自检 `await hudDone()`。

方法论上的教训比这四条更重要：**我连着三次拿 `exit=0` 当成「画出来了」**。渲染类的东西唯一有效的验收是截图——`screencapture -x -T 2 -R x,y,w,h` 配合后台跑的进程，几秒就能确认，比读三遍代码可靠得多。凡是「输出到屏幕/网络/文件」的功能，验收标准必须是**从输出端观察到**，不是从调用端返回成功。

### 反复出现的一类：静默失败伪装成正常

这个项目里踩到三次，形态一样——**没有报错，功能就是不工作**：

1. 改名后 statusLine 路径失效 → 那个会话永远不上报额度
2. hooks 指向 npm 缓存目录，路径失效后**所有 hook 静默失败** → 再也收不到通知
3. 渲染函数抛异常 → 页面保留上一个 tab 的内容，看起来正常只是内容对不上
4. 隧道进程死了但 `publicBaseUrl` 还在盘上 → 二维码照常生成，扫了打不开
5. IP 变了但套接字还绑在旧地址上 → 进程活着、healthz 照常回 ok，手机连过来是超时
6. `bind` 绑失败 → 服务照常运行，`status` 显示「运行中 ✓ 已信任」，只有日志里一行 `EADDRNOTAVAIL`
7. SSE 断开 → 页面停在最后一帧，倒计时归零还挂着待审批，看起来像服务卡死
8. 刘海 HUD 写完了、模块注释也写了，但 `notify()` 从头到尾走的是 `display notification` ——**代码在，就是没接线**。日志照打 `[notify] ...`，测试全绿，只有肉眼看屏幕才知道弹错了地方

共同点是**失败路径上没有任何可见信号**。加功能时值得多问一句：这东西坏掉的时候，用户会看到什么？如果答案是「和正常一样」，就得主动加一个信号。

第 8 条是新的一种形态，值得单独记：前七条是**运行时**失败得静悄悄，这条是**根本没被调用**。新写一个模块很容易停在「它自己能跑」，而漏掉「有没有人调它」——单元测试只证明前者。防的办法是把「谁调用谁」也变成断言：`test/notify.test.mjs` 把两个通道做成可注入参数，专门测「样式设成什么就必须走到哪个通道」。凡是新增一条输出路径（通知、日志、上报），都该问一句：**如果我把它整个删掉，有测试会红吗？**

第 4 条还有一层更一般的教训：**持久化的配置引用了临时的资源**。凡是写进 `config.json` 的东西，都要问它的寿命和进程/网络的寿命是否一致——不一致就不能无条件信任盘上的值，得在用之前验一次活性。`bind` 是同一个病的另一种形态：把「每次启动探测」的结果当成「用户的配置」存了下来。

### 竞品的 Fixes 段比它的 Features 段有用

对着 vibeisland.app 的更新日志逐条比对，挖出了三个 clamicro 的真 bug
（默认值被固化、两条写路径、deepMerge 共享 DEFAULTS 引用）。

方法很简单：**只读 Fixes，不读 Features。** 功能列表是「他们做了什么」，
和你的取舍未必相干；而每一条 fix 都是这个问题域里**真实踩过的坑**，
只要机制相通，你多半也踩得着。他们在五个版本里反复修「hook 被覆盖后自愈」，
那不是巧合——`~/.claude/settings.json` 是共享文件，谁都会被覆盖。

对不上的直接跳过（他们 30+ agent 集成、SSH 远程，与本项目无关），
对得上的立刻去代码里查一遍，别猜。

---

### 排查时先确认「你以为在观察的东西」真的在被观察

第 6 条查了整整两天，中间给出过一个自信但错误的结论：「办公网络禁止设备互通」。它建立在两个对照实验上，**两个都是无效的**：

1. `python3 -m http.server 9999` 也连不上 → macOS 应用防火墙按二进制放行，`node` 在名单里而 `python3` 不在。这个对照组测的是一个被防火墙拦掉的进程
2. 换手机热点也连不上 → 服务仍绑在旧网络的 IP 上（正是第 6 条那个 bug），而且新网络还会被信任闸门挡回回环。**无论网络通不通都不可能成功**

而三天前的对话里就有直接反证：手机曾经拿到过 `{"error":"not found"}`——那是**服务返回的 HTTP 响应**，证明连得上。当时甚至明确写过「客户端隔离的担心可以排除了」，后来却把它当成了结论。

几条可复用的：

- **对照组必须和被测对象走同一条路径**：同一个二进制、同一种绑定方式、同样的信任状态。差任何一项，它就只是另一个未知数
- **先问「服务到底绑在哪」，再问「网络通不通」**。`lsof -nP -iTCP:<port> -sTCP:LISTEN` 一条命令，第三天才想起来跑
- **「昨天还可以」是最强的线索**：它说明有个变量变了，而不是某个东西天生不行。翻历史记录比继续推理有用

---

## 2026-08-10 这一轮：七个 bug 和四条结论

一天里修的，放在一起看比分开看有用——**七个里有五个是「加防护时新引入的」**。

### 修过的

| # | Bug | 为什么没被早点发现 |
|---|---|---|
| 1 | **默认值被固化进盘**（三重）：`saveConfig` 写整个合并对象；`loadConfig` 的 dirty 路径绕过 `saveConfig` 直接 `writeFileSync`；`deepMerge` 对 override 里没有的键共享 `DEFAULTS` 引用，于是迁移代码一句 `config.notify.macNotify = …` 就**改掉了 DEFAULTS 本身** | 改 DEFAULTS 从来没人验证过「老用户拿到了吗」。发了 570s→180s 和 notifyAutoApproved 之后，**一个老用户都没拿到**，而 README 已经照新默认写了 |
| 2 | **审批详情泄漏主令牌**（提权） | 前一天刚给事件流加了抹除，`ApprovalStore` 漏了。而审批详情才是手机上最主要显示的东西，命令里带凭证又是日常 |
| 3 | **只读命令改写 `settings.json`** | `healHooks()` 放在一次性命令处理之前，`clamicro devices` 也会改文件并留备份，反复破坏就无限堆积 |
| 4 | **孤儿进程画不出任何窗口** | 服务被 `nohup … & disown` 起，PPID=1，拿不到图形会话。窗口建得出来、`isVisible` 为 true、退出码 0，**屏幕上什么都没有**；而 `afplay` 不需要 WindowServer，声音照响 |
| 5 | **`?t=` 不认设备令牌**，且登录 cookie 一律写**主令牌** | 手机换设备/清数据后重登走的正是这条路；而主令牌 `forget` 吊销不掉，每设备令牌的设计因此形同虚设 |
| 6 | 通知正文是助手回复的 **markdown 原文**（屏幕上出现「## 改了什么…」） | 没人从「用户眼睛」这一端看过通知 |
| 7 | **静默降级不告知**：刘海画不出来自动回落横幅，而 `status` 一个字不说 | 降级逻辑是我自己加的，加完就默认它"透明" |

### 结论

**一、加防护本身会引入新的失败面，而且往往和被防护的问题同类。**
2、3、7 都是修 bug 时新引入的。给事件流加抹除 → 审批那半忘了；加 hook 自愈 → 只读命令有了副作用；加自动回落 → 降级变得看不见。**每加一层防护，都要问一遍「它自己坏掉时是什么样」。**

**二、`exit=0` / 日志有输出 / `isVisible=true`，都不能证明用户看见了。**
第 4 条连着三次拿退出码当验收，实际一次都没显示过。渲染类功能唯一有效的验收是截图：`screencapture -x -T 2 -R x,y,w,h` 配合后台进程，几秒就能确认。

**三、控制组必须走和被测对象同一条链路。**
第 4 条查了很久，因为我总用 `test-push`（我 shell 里的短命进程）或直接跑 `osascript` 验证——**两者都有活着的会话祖先，而服务没有**。同一份代码，两条路径结果相反。这条 NOTES 里早就写过（`.local` 那次），又犯了一遍。

**四、竞品的 Fixes 段比 Features 段有用。**
第 1、4 条是对着 vibeisland.app 的更新日志逐条比对挖出来的。方法：**只读 Fixes，跳过 Features**。功能列表是"他们做了什么"，和你的取舍未必相干；而每条 fix 都是这个问题域里真实踩过的坑，只要机制相通你多半也踩得着。他们在五个版本里反复修「hook 被覆盖后自愈」——那不是巧合，`~/.claude/settings.json` 是共享文件。

### 附：刘海统一化，三条路都实测否掉

| 方案 | 结果 |
|---|---|
| LaunchAgent（`gui/$UID` 域） | ❌ agent 确实跑了（日志 `ppid=1`），屏幕上没有 |
| 最小 `.app` + `open`（走 LaunchServices） | ❌ app 确实启动了，屏幕上没有 |
| `launchctl asuser` | ❌ |
| 对照组：活着的 shell 直接跑 | ✅ |

判据只有一个：**父进程是不是 launchd**。三种「理论上有图形会话」的机制全部无效。
我原以为 LaunchAgent 的 `gui/` 域能解决——**那个判断是错的，已用实验推翻**。
真要统一走刘海只剩「编译签名的 GUI app」一条路，和「零依赖、`npx` 装完即用」冲突。

### 附：工具本身也会骗人

这台机器的 `grep` 是 **ugrep 别名**，核对发布产物时对某些模式静默返回空——差一点把「已修复」当成「没修复」。
**核对关键结论时换一种工具再验一遍**（比如用 python 逐字节比对）。

---

## 2026-08-23 这一轮：接 Codex，和它顺手掀出来的一批

接 OpenAI Codex（ChatGPT 的 CLI）花的时间远少于预期——0.147 起它自带的 hook
系统跟 Claude Code 几乎同构：事件名一字不差、payload 字段一样、输出也是同一套
`hookSpecificOutput`。真正花时间的是**它掀出来的东西**。

### 三个「写了但没人读」

| 字段 | 承诺 | 实际 |
|---|---|---|
| `agents.mjs` 的 `approve` | 「这个后端还没开审批」 | 全仓库没有任何代码读它。于是 Codex 的 PermissionRequest 一接上，审批就真的跑起来了——而它的 deny 线格式还没在真机上验过 |
| `network.mjs` 的 `weak` | 「让确认那一步能告诉用户这个网络不好认」 | 同样没人读。那句承诺一次都没发生过 |
| `state.mjs` 的 `pending_approval_id` | 「这个会话此刻卡在哪」 | 有人读，但并发时被后来那条覆盖，指向的不是最该看的那条 |

前两个是同一种病：**加了一个字段，没有任何代码消费它**，而注释把它写得像是已经
生效了。写下来的承诺不会自己实现。加字段时要当场回答「谁读它」，答不上来就
别加——一个只写不读的安全提示，比没有这个提示更糟，因为它让人以为有。

### 一个差点上线的、比要修的 bug 更严重的修复

给「决定送没送到」加检查时，判据写成了 `req.destroyed`。而 `readBody()` 把请求体
读完之后 Node 会**自动 destroy 可读侧**——于是一个完全健康的请求在那一刻
`req.destroyed` 就是 true。后果是每一条审批都短路、永不写响应，**审批全线失效**。

判据必须落在 `res` 上。这条的教训不是「用错了字段」：

> **拿框架对象的状态当判据时，先验证它在正常路径上的值。**
> 我假设 `destroyed` 只在异常时为真，没验证过它在成功路径上是什么。
> 一行 `console.log` 就能问出来，而我直接写进了安全边界。

而它是怎么暴露的也值得记：**测试挂住了**。不是断言失败，是超时。挂住比红更糟——
我一开始以为是环境问题。现在 `http.test.mjs` 里有一条断言直接盯着后果
（「决定明明送到了，却被记成没送到」），下次不会再靠超时发现。

### 结论

**一、`2026-08-10` 那条「加防护会引入新失败面」，今天又成立了一次，而且更险。**
上次是三个新引入的失败面，这次是一个差点让核心功能全废的。规律稳定到可以当
前提用：**每写完一个防护分支，先假设它自己是错的，问「它误判时会发生什么」。**

**二、成对事件 + 并发 = 必须计数。**
`clearWaitingApproval` 是布尔思维遇上并行工具调用。凡是「开始 X / 结束 X」成对
出现的状态，只要来源可能并发，就得引用计数。这条对 `markWaitingApproval`、
`control` 的 waiter、将来任何「有几条挂着」的地方都成立。

**三、沉默不等于故障，也不等于正常——只能如实说「没动静」。**
被 kill 的会话和跑二十分钟测试的会话，在数据上长得一模一样。把前者判成「出错」
和把后者显示成「运行中」是同一类错误，只是方向相反。能说的只有观察到的事实：
「已经 42 分钟没有任何上报」。用户知道自己有没有在跑长命令，我们不知道。

**四、整份快照写盘 = 后写者赢。**
短命 CLI 和常驻服务写同一个文件，各自持有启动时的快照。窗口不是微秒级——
`trust` 要算网络指纹（`ping -W500` 加几个 spawnSync），几百毫秒到一秒，而
「一边在终端敲 trust 一边拿手机扫码」正是标准装机流程。现在 `saveConfig` 支持
点名写（`{ only: [...] }`），只对自己那一项负责。**真正的解法是让跑着的服务当
唯一写者、CLI 走 API**，那还没做。

**五、文档里最该盯的不是数字，是「这句话现在还成立吗」。**
`docs.test.mjs` 和 `site-numbers.test.mjs` 已经把默认值和数字钉住了，今天一条都
没红。而实际过时的是**语义**：SECURITY.md 里「装机会写哪些文件」少了第三个文件、
官网少了一个后端、指南的「最该看的是命令原文」在写文件类操作上不再完整。
这类只能人工逐份对。

## 2026-08-27 这一轮：一次审查，和它掀出来的六个

起因是「找出系统 bug」，没有具体线索。结论比 bug 本身有用：

### 1. 最贵的那个洞在 try 外面

`new URL(req.url, 'http://' + host)` 裸奔在 handler 的 try **之外**。`Host: [`
让它抛 ERR_INVALID_URL，而这是 http 回调里的同步抛出——uncaughtException，
进程当场退出。

要命的是**位置**：它跑在 hostAllowed、isLoopback、authorized **全部之前**。
三道闸门一道都拦不住，任何能对端口开一条 TCP 的人发四个字节就能杀掉服务。

> **闸门只保护它后面的代码。** 排查一个「谁能打到这里」的问题时，
> 要从进程边界往里数第一行，不是从第一个 if 往下数。

### 2. 「界面说的和事实相反」这次出现在日志里

netwatch 收缩关的是 `config.lanIp`——启动那一刻探到的值。换过一次 IP 之后，
还活着的套接字已经不是它了：关掉一个早就没用的旧 socket，新地址上的监听原样
留着，而日志和通知都写着「已摘掉局域网监听」。

同一段代码的另一半：`ALLOWED_HOSTS` 启动时算死，于是「已恢复局域网监听」
之后手机连过来 Host 对不上，一律 403。**日志说恢复了，实际连不上。**

> 两个 bug 同源：**「监听在哪」和「白名单认哪」是同一件事，分两处写就会有
> 一处忘记跟。** 修法不是各修各的，是把它们收进一个函数，外面不留第二个入口。

### 3. 转义和截断的顺序

`replace(...).slice(0, 200)` —— 转义会让字符串变长，那一刀可能落在 `\"`
中间，留下一个孤立的反斜杠把收尾引号吃掉。真 `osacompile` 扫 120 种对齐：
**修前 48 条语法错误，修后 0**。而通知正文就是命令原文，引号遍地。

同样的顺序在 `confirm.mjs` 里还有一份（那边够不着，是颗没踩到的雷）。

> **先截断，再转义。** 反过来永远是错的，只是不一定看得见。

### 4. 它能潜这么久，是因为失败被记成了成功

`notifyBanner` 的 `p.on('close', resolve)` 不看退出码。语法错误、二进制不在、
被安全策略拦下，全都算「发出去了」。于是整套健康计数
（`consecutiveFails` / `lastError`，设置页就靠它）**对横幅通道从来没生效过**：
48 次真实失败，日志一行没有，`failed` 一直是 0。

> 这条在这个仓库里已经是第 N 次了（HUD 在孤儿进程里退出码 0、屏幕上什么都
> 没有）。**没抛异常不等于送达了**——每加一个 spawn，先问它非 0 退出会怎样。

### 5. 「已安装」不是一个数

要在 dashboard 上显示 skill 数量。盘上数出来五个答案：

    ~/.claude/skills/*                      4   ← 你自己装的
    已启用插件带的                          30
    plugins/cache/** 全部                   48   ← 含没启用的插件
    plugins/marketplaces/** 里的            31   ← **商店目录，压根没装**
    盘上 SKILL.md 总数                      83

只有前两个是真的。而且写的过程里自己踩了两个：递归找 `skills` 目录会把插件
仓库**自己开发用的** skill（`release`、`plugin-audit`）算进来，多 7 个；
按目录数而不是 `SKILL.md` 在不在数，多 1 个。

> 显示一个聚合数字之前，先把它拆开数一遍。**拆不开的数字通常是错的**，
> 而且错得看不出来。

### 6. 加一档新值，暴露了一直存在的老错

主控台是 `esc(s0.sub_state)` 裸输出，写着 `Editing`，而正下方的卡片写「编辑中」
——同一个会话、同一屏、两种叫法。`ui/agents.js` 那份共享表的开头讲的就是这类
事故，可它只钉了「表定义在一处」，**没钉「用到的地方都去查表」**。

加 `Using Skill` 那一档才被看见：英文夹在中文里太扎眼，而 `Editing` /
`Thinking` 长得像专有名词，错了好久没人当回事。

> 一致性测试要钉两头：定义在一处**且**用的地方都去查它。只钉前一半，
> 漏掉的那一处会安静地一直说错。

### 7. 发出去当天就撞回来的那个：一无所知 ≠ 没人在用

2.17.0 发布后一小时，服务在我正用着 Claude Code 的时候自己关了。**两次。**
日志留着：

```
14:44:03 [clamicro] 监听 http://127.0.0.1:8765     ← 服务重启
14:49:03 [lifecycle] 没有后端在用了（no-owner-known），再确认一次就退出
14:54:03 [lifecycle] 所有后端都退出了，服务关闭
```

一行就能证：

```js
shouldExit([], { owners: [] })  →  { exit: true, why: 'no-owner-known' }
```

`known === 0` 的意思是「我什么都不知道」，不是「我确认过没人」。而
`lifecycle.mjs` **自己的注释开头就写着「不确定就不退」**——代码和它声明的
原则是反的，而且是我自己写的那段注释。

触发路径：会话表和 owners 都不落盘，所以服务因为任何原因重启后就一无所知；
而 `SessionStart` 一个会话只发一次，早就过去了。于是接下来十分钟没有 hook
到达——用户在读代码、在想事情、在开会——服务就走了。原本设计里那道
`unknown-owner` 拦不住：它要求**会话记录存在**，而重启后那张表是空的。

后果不是「多重启一次」：九个 hook 全部连不上，而 hook 失败被 Claude Code
当成**非阻塞错误**——`PermissionRequest` 不再阻塞，本该上手机等你批的操作
直接跑过去，手机上什么都不显示，终端里也没有报错。

> **写「不确定就不退」和真的不退是两件事。** 每一个「兜底分支」都要问一遍：
> 它在**信息为零**时走哪边？零信息是最常见的运行状态（刚启动、刚重启、
> 刚升级），不是边角情况。

#### 两处修法，一处兜底一处治本

  · **兜底**：`known === 0` 时不退。零信息只让它继续跑，代价是一个闲置进程
  · **治本**：`bin/statusline.sh` 也带 `?owner=$PPID`。SessionStart 一个会话
    只发一次，而 statusLine **每回合都跑**——重启后一个回合服务就重新认识
    宿主，不必等下一次开会话

**没有把 owners 落盘**，虽然那看起来更彻底。盘上的 pid 跨重启就没有意义了
——PID 会被系统回收，这个仓库已经为此栽过两次（`service-id.mjs` 的 stop、
`config.mjs` 的 `tunnelAlive`，两处都写着「PID 本身从来不足以标识一个进程」）。

#### 最贵的一条教训在测试里

那两条挡在前面的测试，名字叫

    ✔ 从没见过任何宿主时**也退**，理由要说得出来
    ✔ 超过陈旧线就不再钉住服务

**它们把 bug 写成了规范，而且是绿的。** 修的时候这两条变红，第一反应会是
「我改坏了」——真正该做的是看一眼它们断言的行为对不对。名字和断言一起改掉了，
免得下一个人照着它再改回去。

> 一条绿测试只证明「行为和当初写它的人想的一致」，不证明那个想法是对的。
> **测试变红时，先问它断言的是不是一个正确的行为。**

### 8. 参考信息要让位，而这条规矩早就写在文件里了

skill 计数做完，手机上实拍一看：一个静止的数字挂在「运行中 · 思考中」正下方，
跟滚动的工具调用抢注意力。

而这一页 `renderTopUsage` 上面明明白白写着：

> 一旦有事在发生（运行/待审批/出错），主控台要让给「正在发生的事」，用量就
> 降级成 header 下面这一条——**它始终有用，但它不该和「此刻要你看的东西」
> 抢位置**

我加新东西的时候没去读那段。而 skill 计数比额度更该靠后：额度至少回答
「还能不能再来一轮」，而「装了几个 skill」是**一天都不会变的事实**。

> 往一个已有的界面里加东西之前，先找一遍**这块地方已经写过的规矩**。
> 那些注释不是给别人看的，是给下一个往这儿加东西的人看的——而那个人是你。

顺带：那段 idle 判据原来内联在 renderTopUsage 里，现在两个消费者了，抽成
`isIdleView()`。各抄一份的话早晚有一处漏掉某个状态，**而漏掉的那一处会在
最忙的时候继续占着位置**。

### 这一轮最后发成了什么

```
2.16.0  起点
2.17.0  Codex 接入 + lifecycle + skill 显示 + 4 个安全修复
        ↳ 发出去当天翻车三次（见第 7 条），已 deprecate
2.17.1  一无所知 ≠ 没人在用
2.17.2  skill 计数只在空闲时显示
```

一天之内三个版本，两个是修前一个。值得记住的不是「发快了」——安全修复本来
就该快——而是**那条分支从没被 CI 跑过**（test.yml 只在 `push: main` 和
`pull_request` 上触发），于是 node 18/20/22 的测试失败和 lifecycle 的误判
都是合并之后才暴露的。

> 长期分支合并前走一次 PR。不是流程洁癖：**CI 覆盖的四个 node 版本里，
> 本地那个恰好是唯一能跑绿的那个。**

### 顺带

这一轮改完补了 96 条测试（1180 → 1276）。其中最该留下的是那条**契约测试**：
「时间线里工具调用那一行不能只有一个工具名」——它钉的不是某个具体串，
而是「加新工具时忘了带字段」这个**形状**，那类漏不会让任何现有测试变红。

另一条经验：`lanbind` 那 23 条测试是把逻辑从 server.mjs 抽出来才写得出的。
**上一轮只修了行为、没修可测性**，而可测性正是那两个 bug 能同时溜进来的原因。

---

## 2026-08-28 这一轮：控制面的诚实，和第四次「绿测试钉着错行为」

### 1. 「取消本轮」承诺了一件它做不到的事

它拦的是**下一次工具调用**。一个不调工具的回合（长文本、规划、纯思考）
它碰不到——架构文档 §5 早写着「单靠 PreToolUse 拦不住一个不调工具的回合」，
而按钮上写的是「取消本轮」。

改名不是措辞洁癖：「取消中」这三个字的意思是**正在停**，用户会盯着屏幕
等它停下来。而实际是「已排好，等下一次工具调用」——可能等很久，也可能
永远不发生。

> **一个控制按钮的名字，是它对用户的承诺。** 做不到的部分要写在名字里，
> 不能写在文档里。

### 2. 跨回合遗留：绑定「这一轮」而不是「这个会话」

armed cancel 原来只是一个布尔，没被消费就一直留着等下一个拦截点——而下一个
拦截点可能是**下一轮的第一条命令**。§6.2.1 记了很久。

修法三道：turnKey 绑定（用 `turn_started_at`，三个后端都有，零新字段）、
45 秒 TTL、回合结束时显式作废（store 新发 `turn-end`，三条路都汇到这里，
接一处全覆盖）。

有意思的是**上游协议也是这么做的**：Codex 的 `turn/steer` 参数里
`expectedTurnId` 是强制前置条件，注释写着「必须匹配当前活动轮次」。
同一个问题，同一个答案。

### 3. 判定「不做某事」时的静默，本身是个 bug

起因是一句提问：「我退出了 Claude Code，发现服务没有关闭」。查下来是
**正确行为**（另一个会话还开着）。但 `maybeExit()` 判定「不退」时完全静默，
只有打算退出才写日志——于是日志里一整天空白，那既可能是「一直有人在用」，
也可能是「巡检根本没跑」，**分不出来**。

> 一个会自动做决定的东西，必须能回答「你现在为什么没做」。只在动手时
> 才说话的设计，把「正常」和「卡住」压成了同一种沉默。

`clamicro status` 现在直说，而且判据不重算——调的就是 `maybeExit` 用的那个
`shouldExit`。两处各判各的，迟早出现「status 说要退、服务不退」。

### 4. 第四次：绿测试钉着一个错行为

这一轮又撞到两条：

    ✔ 已请求取消，在它落地之前也要说出来   ← 断言 stateLabel === '取消中'
    ✔ 目录整个不存在 → 数不出来（null），不是 0

第二条尤其值得说。它是**我三天前自己写的**，而且当时写得理直气壮——
文件顶上就写着「`0` 和「数不出来」必须分得开」。但实现里 `readdirSync` 的
catch 把「目录不存在」和「读不动」并成了同一个 null，注释甚至把两种情况
并列列出来了，却给了同一个值。

后果：**每一个还没装过 skill 的人**（`~/.claude/skills` 不存在是全新安装的
常态），首页永远挂着一句「你的 skill 数不出来」——看起来像故障。

> 把「没装」说成「数不出来」，和把「数不出来」说成「0」**一样是撒谎**，
> 只是方向相反：前者把正常说成故障，后者把故障说成正常。**两个方向都要防。**

而这个 bug 是「确认没问题再提交」那一步抓到的——单测 1311 全绿，是在一个
干净的临时 HOME 上打开首页才看见的。

### 5. 记下那条没走的路

查证了 Codex app-server 的完整协议（`generate-json-schema`，292 个 schema、
95 个方法）：`turn/interrupt` 和 `turn/steer` 都在，正是「立即中断本轮」和
「真中途插话」需要的东西。

**仍然决定不接**，理由是爆炸半径：那条连接依赖的 daemon 和桌面 ChatGPT 共用，
我们的 bug 会波及用户正在用的会话。

> 记「为什么不做」比记「做了什么」更值钱。不记的话，下一个人会从
> 「协议支不支持」重新论一遍——而那个问题的答案是「支持」，
> 于是他很可能得出相反的结论。

写进了架构文档 §2.2。

### 6. 47 个函数，1 个跑过

搬之前的 `ui/home.html`：2841 行，其中 2057 行 JS，47 个顶层函数，全挤在
同一个扁平作用域。**其中被任何测试真正执行过的只有一个**——`agentUsage`，还是靠
test/agent-usage 里手写的 `extractFn()` 按文本抠出来塞进 vm 才跑起来的。
其余 46 个，测试对它们做的只有 `readFileSync` + 正则。

那不是覆盖率数字难看的问题。正则扫得出「这段字在不在」，扫不出「这段字
跑起来对不对」，而这一轮三个「1300 条测试全绿、界面是坏的」全出在这个文件：

| 症状 | 真因 | 有没有测试变红 |
|---|---|---|
| 纯 Codex / 纯 DSH 用户打开会话 tab 就是「页面渲染出错」 | 两个同名的 `staleNote`，后声明的静默赢 | 没有 |
| 会话 tab 整个空白 | 删 `qbadge` 时留下 `const qn = Object.values(inbox)`，而 inbox 已经没了 | 没有 |
| 主控台写 `Editing`，正下方的卡片写「编辑中」 | `esc(s0.sub_state)` 裸输出，没查表 | 没有 |

三个都只有**真把页面打开**才看得见。

所以开了 `ui/view.js`，先搬五个纯判断（`sessionStaleNote` / `ago` /
`quotaStale` / `quotaStaleNote` / `isIdleView`）——正是这轮出事的那几个。
判据是**能不能在一个空的 vm context 里跑**：不碰 DOM、不读全局，要什么
就作为参数传进来，`now` 一律可注入。留在 home.html 里的只有一个绑定器
`viewState()`，名字故意跟那边不重。

搬完补了 31 条**执行级**测试。四个变异各自验过一遍确实会红：页面重定义
同名函数、漏 `<script src>`、漏 pages.mjs 白名单、`quotaStale` 把「没拿到过」
也算成过期。

> `quotaStaleNote` 原来直接读 `quota` 这个全局。想测它就得先把整个
> home.html 的作用域搭起来——**而那正是它一直没有测试的原因**。
> 一个函数难测，通常不是测试的问题。

不必一次搬完。改到哪个搬哪个，每搬一个「不可测的全局」就少一个：
这一次是 2841 → 2755 行、47 → 42 个，而**跑得起来的从 1 个变成 6 个**。

### 7. 会话页：最显眼的两个按钮都是「让它停」

这一页原来的底栏是「暂停」「取消下一步」两个通栏按钮，而发消息在时间线
**下面**——长会话里十几屏之外。于是屏幕上常驻可见的操作全是让它停，
「跟它说一句」得先翻到底才知道存在。

先试过加一颗跳转胶囊（看不见输入框就写「↓ 发消息」，看得见就写「↑ 最新」）。
做的过程中踩了两脚，都值得记：

- **在 render() 末尾同步量一次「输入框在不在视野里」，逻辑对、时机错。**
  短会话（一屏装得下）里那一眼看早了，算出「看不见」，而此后页面根本不滚，
  没有第二次机会——结果就是一颗写着「↓ 发消息」的胶囊浮在一个整屏可见的
  输入框旁边，点下去什么都不发生。事后手动再调一次它就正确地消失了。
- **改用 IntersectionObserver 之后，它在后台标签页里一次都没回调**——连初始
  那次都没有。同一个环境里 `requestAnimationFrame` 也是挂起的。

> 一个我**没法在本地验证**的机制，不该成为「让功能被发现」的唯一依靠。
> 最后回到自己量，但把所有会改变答案的时刻都接上（下一帧、滚动、resize），
> 而且同步先量一次——rAF 不跑的环境里那一次就是唯一的一次。

顺带钉死一条：`behavior:'smooth'` 不是到处都灵。iOS Safari 15.4 之前
`scrollIntoView` 的 smooth **整个调用被忽略**，点了原地不动；本机这个浏览器
也复现了。跳转类的操作一律直接赋 scrollTop。

然后用户说得更直接：**页面直接变成聊天输入框，暂停/取消变图标。** 胶囊
连同「跳到发消息」一起没了——输入框常驻在底栏，没有可跳的地方。

**但它长得像聊天框，不是聊天框。** 注入的唯一时机是 Stop hook（这一轮结束
时）；会话空着的时候更远，得等你在终端里再发起一轮。聊天的形状自带一个
承诺——打完字按下去对面就收到了——而版式越像，那个承诺越结实。

> 所以输入框底下钉着一行小字说什么时候到，空闲时换成更响的一句。
> **那一行任何时候都不能省**：它是这个形状唯一拆得掉自带承诺的东西。

按钮上的字也改了：「加入队列」→「发送」。前者是从内部机制反推出来的词，
得先知道有个队列才知道自己在干嘛；而「不会立刻送达」下面那行已经说了。

### 8. 你每调一次工具，我正在打的字就被删掉重建一次

这一页的发消息整块原来每次 render() 都 `innerHTML = ...` 重刷，而 render()
是 SSE 驱动的——`session` 和 `event` 两个事件都直接调 refresh()。

实测（推一条 PreToolUse 进去的前后）：

    activeElement   draft → BODY
    selectionStart  5 → 0

文字本身没丢（有个 `draft` 全局兜着），但**光标跳回开头**，接着敲的字插在
句首；手机上还会顺带收起键盘。而这一页的用法恰恰是「一边看它在干什么一边
打字」——事件越密被打断得越狠，正好是最需要说话的时候。

修法是把外壳和内容拆开：textarea 写死在 HTML 里、事件只绑一次，每次 render
只改会变的那几样文字（送达提示、错误、队列），一个都不含焦点。

> 「重新渲染」在无状态的地方是免费的，在**装着用户正在输入的东西**的地方
> 是破坏性的。焦点、光标位置、输入法的候选框——这些都不在你的 state 里，
> 但它们都在用户的脑子里。

### 9. 开一次页面 105 个请求

两个页面都把 SSE 直接接到刷新上：

    es.addEventListener('event', refresh)      // ui/session.html
    es.addEventListener('session', refresh)    // ui/home.html

而 `/api/stream` **连上的那一刻会把历史事件全量重放**（`store.events(lastId)`，
首次连 lastId=0，上限三千条）。实测：

| | 改之前 | 改之后 |
|---|---|---|
| 会话页开一次（85 条事件） | **105 个** `/api/sessions/:id` | — |
| 会话页开一次（273 条事件） | — | **1 个** |
| 首页 20 次工具调用 | **60 个**请求 | — |
| 首页 30 次工具调用 | （会是 90 个） | **3 个** |

每个请求都拉完整负载、都重画整条时间线。真实的长会话在服务端能存到两千条，
那就是开一次页面几千个来回——在手机上，走 Wi-Fi。

**而且这不只是浪费，还是错的。** 同一页并发发 40 次，实测回来的顺序是
`1, 2, 3, 14, 15, 4, 16, 5…`——第 14 个比第 4 个先到。最后落地的那一份不保证
是最新的那一份，屏幕上可以稳定停在一个**比已经收到过的还旧**的状态，
而且没有任何报错：页面在刷新，内容是过期的。

> home.html 早就为**重绘**写过合并（`scheduleRender`，注释写着「逐条重绘既卡
> 又没意义——中间那些帧没人看得见」），只是没往前挪一步管到**请求**。
> 同一句话对网络更成立：中间那些响应不但没人看，还每个都要过网。
> **一条已经想明白的道理，要顺着它一路走到头。**

抽成 `ui/sched.js` 的 `coalesced(run, {raf})`，两个页面共用，19 条执行级测试
（raf 换成手动泵，不靠真等一帧）。两件事分开：

- **合并**只对付机器造出来的洪水（SSE 重放、连续几十个工具调用）
- **`now()`** 给人触发的那一次：只有一次，不需要合并，而且调用方要 `await`
  它。走排程版的话 `await` 立刻返回，代码读起来像等过了其实没有——首页首屏
  的 `refresh().then(connect)` 更是当场 TypeError

首屏和「回前台补一次」也走 `now()`：rAF 在后台标签页是挂起的，而「补上错过
的那些」正发生在刚从挂起里出来的那一刻，让它去等一帧等于把补课托付给刚醒的
那个机制。

> **合并省的是重复，不该省掉唯一的那一次。**

顺带：`/favicon.ico` 一直没有路由。浏览器每开一页都不问自取地要一次，于是
控制台常年挂着一条红色 404——排查别的问题时先得把它认出来再排除掉，而它
什么都不说明。映射到已经在盘上的 icon-180.png 就完了。

---

## 安全边界

局域网内是**明文 HTTP**。同网络的被动嗅探者能拿到 token，进而获得完整控制权。这是设计上接受的边界，不是待修的 bug——修它需要 TLS，而自签证书会让 Safari 报警并破坏「扫码即用」。

缓解手段是网络信任闸门：陌生网络默认只绑回环。真需要在不可信网络上用就装 Tailscale，服务会自动识别 `100.64/10` 并绕过闸门（覆盖网自带加密）。

已实现的防护和各自挡住什么，见 [README 的安全章节](./README.md#security)。加新端点时注意：

- `/hooks/*` 和 `/statusline` **必须**只接受回环来源（它们没有 token）
- 任何新页面都受全局 Host 白名单保护，但新的**未认证**端点要单独想清楚 CSRF
